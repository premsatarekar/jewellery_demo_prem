import React from 'react';
import CustomerDetails from './CustomerDetails';

const CustomerDetailsWrapper = ({ sales }) => {
  return <CustomerDetails sales={sales} />;
};

export default CustomerDetailsWrapper;
